# Copy to deploy/.env.local (gitignored) and fill in the three box
# SSH targets. push-deploy.sh reads these; never commit them.

BOX_A_SSH=user@<box-a-public-ip>
BOX_B_SSH=user@<box-b-public-ip>
BOX_C_SSH=user@<box-c-public-ip>
