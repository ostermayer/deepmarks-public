{
  "name": "deepmarks-bunker",
  "version": "0.1.0",
  "description": "Box C — NIP-46 signing service. Holds operational nsecs and signs allowlisted event kinds on request from payment-proxy.",
  "license": "GPL-3.0-only",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@noble/hashes": "^1.4.0",
    "dotenv": "^17.4.2",
    "fastify": "^5.8.5",
    "nostr-tools": "^2.7.2",
    "pino-pretty": "^13.1.3",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/ws": "^8.5.10",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.4"
  },
  "engines": {
    "node": ">=20"
  }
}
